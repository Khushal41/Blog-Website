<!DOCTYPE html>
<html lang="en">

<head>
  <!-- Declares the document as HTML5 and sets the language to English -->
  <meta charset="UTF-8">
  <!-- Sets the character encoding to UTF-8 to support a wide range of characters -->

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Ensures the webpage is responsive, adjusting the layout based on the device's screen size -->

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <!-- Links to the Bootstrap 5.3.2 stylesheet for pre-built styles and components -->

  <title>Blog Website</title>
  <!-- Sets the title of the webpage, which appears on the browser tab -->
</head>

<body>
  <!-- Main body of the webpage -->

  <div class="container">
    <!-- A Bootstrap container class that centers and adds padding to the content -->

    <h1 class="mb-1">
      <%= article.title %>
    </h1>
    <!-- Displays the title of the article dynamically using EJS. The margin-bottom (mb-1) adds spacing below the title. -->

    <div class="text-muted mb-2">
      <%= article.createdAt.toLocaleDateString() %>
    </div>
    <!-- Displays the creation date of the article in a human-readable format (using toLocaleDateString()).
             The text is muted (light gray) and has margin-bottom (mb-2) for spacing. -->

    <a href="/" class="btn btn-secondary">All Articles</a>
    <!-- A button styled with Bootstrap's secondary color (gray), linking back to the main list of articles (homepage). -->

    <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
    <!-- A button styled with Bootstrap's info color (light blue), linking to the edit page of the current article, using its ID dynamically in the URL. -->

    <div>
      <%- article.sanitizedHTML %>
    </div>
  </div>
  <!-- End of the container div -->
</body>

</html>