<!DOCTYPE html>
<html lang="en">

<head>
   <!-- Declares the document as HTML5 -->
   <meta charset="UTF-8">
   <!-- Sets the character encoding to UTF-8, which supports a wide range of characters -->

   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <!-- Ensures that the page is responsive, adjusting its layout based on the screen size (especially for mobile devices) -->

   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
   <!-- Links to the Bootstrap 5.3.2 stylesheet for pre-built responsive design and UI components -->

   <title>Blog Website</title>
   <!-- Sets the title of the webpage that appears on the browser tab -->
</head>

<body>
   <!-- Main body of the document -->

   <div class="container">
      <!-- Bootstrap container class that centers and provides padding for the content -->

      <h1 class="mb-4">Edit Article</h1>
      <!-- Page header with a margin-bottom of 4 units (mb-4) for spacing -->

      <form action="/articles/<%=article.id %>?_method=PUT" method="POST">
         <!-- 
            Creates a form that sends a POST request to update an article with a specific ID. 
            The action sends the request to "/articles/[article_id]", where <%= article.id %> is dynamically inserted.
            The "_method=PUT" is often used in Node.js (with method-override) to simulate a PUT request, as HTML forms only support GET and POST by default.
            -->

         <%- include('form_field') %>
            
      </form>
   </div>
   <!-- End of the container div -->
</body>

</html>